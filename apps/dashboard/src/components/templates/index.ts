/**
 * Template-tier components for the Nothing-aesthetic dashboard.
 *
 * Templates compose organisms + molecules + atoms into full page chrome.
 * They MUST NOT import from adapters/ or application/ — that's the
 * composition root's responsibility (the page itself).
 */
export { DashboardLayout } from "./DashboardLayout";
export type { DashboardLayoutProps } from "./DashboardLayout";
