/target
*.swp
*.swo
.env
.claude/worktrees/

# Session state — sentinel writes these per-session, never commit
.claude/scheduled_tasks.lock
.claude/settings.local.json

# Auto-managed by hooks — content is current state, snapshots rot
/tasks.md
/BLOCKED.md

.commit-msg.tmp

# Transient build-log redirects (e.g. `cargo build … > eng-build.log`)
*.log
