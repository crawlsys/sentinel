# Tailscale Skill — Step Definitions
# Tailscale network connectivity and file transfer
#
# 2 phases, ~6 steps

# ============================================================================
# Phase 1: Connect — Verify Tailscale network path
# ============================================================================

[[phases]]
id = "connect"

[[phases.steps]]
id = "1.1"
description = "Check Tailscale daemon status"
blocker = true

[[phases.steps]]
id = "1.2"
description = "Identify target device on tailnet"

[[phases.steps]]
id = "1.3"
description = "Verify network path and connectivity"

# ============================================================================
# Phase 2: Transfer — Send or receive files
# ============================================================================

[[phases]]
id = "transfer"

[[phases.steps]]
id = "2.1"
description = "Send or receive files via Taildrop"

[[phases.steps]]
id = "2.2"
description = "Verify transfer completed successfully"

[[phases.steps]]
id = "2.3"
description = "Report transfer summary"
